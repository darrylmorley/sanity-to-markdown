/**
 * Example project.
 * Replace the projectId and dataset with your own.
 **/

const config = { projectId: "your project id", dataset: "your dataset name, default:production", token: 'you-sanity-api-token', useCdn: true };

module.exports = config;
